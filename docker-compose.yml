version: '3.8'

services:
  voidloader:
    build:
      context: .
      dockerfile: Dockerfile
      target: backend
    ports:
      - "5000:5000"
    volumes:
      - ./server/downloads:/app/server/downloads
    environment:
      - PORT=5000
    restart: unless-stopped
